After installing the pre-requisites:

1. Create a cluster in AWS EKS using the below commend. Here I created a cluster with with serverless compute "FARGATE".
$ eksctl create cluster --name game-cluster --region ap-south-1 --fargate

